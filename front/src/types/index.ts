/**
 * types/index.ts - Point d'entrée des types TypeScript
 * 
 * Ce fichier ré-exporte tous les types depuis types.ts
 * pour permettre des imports simplifiés dans l'application.
 * 
 * @example
 * ```tsx
 * // Au lieu de :
 * import { HeroContent } from '../types/types'
 * 
 * // On peut écrire :
 * import { HeroContent } from '../types'
 * ```
 */
export * from './types'
